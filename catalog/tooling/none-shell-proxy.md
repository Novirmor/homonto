No shell proxy is declared. Run shell operations directly; there is nothing to
probe and nothing to warn about. To route them through a token-optimizing
proxy, set `shell_proxy` under `[tooling]` in `homonto.toml` and re-apply.
